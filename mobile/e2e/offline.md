# Offline synchronization E2E acceptance scenario

- Start a rehearsal and disable network connectivity before saving the completion.
- Verify the completion is stored locally with a visible pending-sync status.
- Restore connectivity and verify automatic synchronization occurs once.
- Relaunch the app and verify no duplicate completion is created and the queue is empty.
